source "http://rubygems.org"
require "rbconfig"

# Will automatically pull in this gem and all its
# dependencies specified in the gemspec
gem "ipc-msgq", :path => File.expand_path("..", __FILE__)

group :default do
  
end # :default do 

gem "changelog"

group :test do
  gem 'rspec', '~>2.2.0'
end